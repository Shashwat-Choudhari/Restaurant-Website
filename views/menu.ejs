<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=PT+Sans+Narrow&family=Rajdhani:wght@300&family=Space+Grotesk:wght@300..700&family=Whisper&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
    <title>Menu</title>
</head>
<body class="Menu">
    <div class="block">
        <div class="horizontal" style="width: 100%;">
            <a href="/"><img src="/home.svg" alt="Home" class="home"></a>
            <p class="heading2">Menu</p> 
            <a href="/cart"><img src="/cart.svg" alt="Cart" class="cart"></a>
        </div>
        <% if(locals.items){ %>
        <% for(var i=0;i<items.length;i++){ %>
            <div class="card">
                <img src="/<%= items[i].filename %>" alt="<% items[i].item %>" height="100px" width="100px">
                <div style="text-align: center;"><%= items[i].item %></div>
                <div>Rs. <%= items[i].price %></div>
                <div>Rating: <% for(var j=0;j<items[i].rating;j++){%>⭐<%} %></div>
                <form action="/cart/<%= items[i].id %>" method="post">
                    <input type="submit" value="Add to Cart" class="addTocart">
                </form>
            </div>  
        <% } %> 
        <% } %>
    </div>
    <form action="/reviews" method="get">
        <input type="submit" value="Reviews" class="submit">
    </form>
</body>
</html>